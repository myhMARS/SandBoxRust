name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y libseccomp-dev

      - name: Build server
        run: cargo build --release -p redbear-sandbox

      - name: Test (unit)
        run: cargo test -p redbear-sandbox

      - name: Lint
        run: cargo clippy -p redbear-sandbox -- -D warnings

      - name: Build seccomp (python3)
        run: cargo build --release -p seccomp_redbear --features python3

      - name: Build seccomp (nodejs)
        run: cargo build --release -p seccomp_redbear --features nodejs

  integration:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4

      - name: Install system deps
        run: sudo apt-get update && sudo apt-get install -y libseccomp-dev python3 nodejs npm

      - name: Build server
        run: cargo build --release -p redbear-sandbox

      - name: Build and setup seccomp libs
        run: |
          sudo mkdir -p /usr/local/share/sandbox/tmp
          sudo chmod 1777 /usr/local/share/sandbox/tmp
          cargo build --release -p seccomp_redbear --features python3
          sudo cp target/release/libsandbox.so /usr/local/share/sandbox/libpython.so
          cargo build --release -p seccomp_redbear --features nodejs
          sudo cp target/release/libsandbox.so /usr/local/share/sandbox/libnodejs.so
          sudo npm install --prefix /usr/local/share/sandbox koffi

      - name: Run integration tests
        run: bash tests/integration.sh
